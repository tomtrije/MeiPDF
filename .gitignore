# Build artifacts
.build/
MeiPDF.app/

# Distributed disk images and local release output (DMG + generated appcast + staging)
Distribution/
.dmgstage/

# Sparkle signing secrets — NEVER commit these.
# Secrets/ed25519_private.key (base64 seed consumed by generate_appcast/sign_update)
# Secrets/ed25519_private.pem (PEM master secret)
Secrets/

# Transient CI / tool files
cert.p12
edkey.txt
*.keychain
*.keychain-db

# macOS
.DS_Store

# WorkBuddy local project data (memory, runtime state) — never commit to the repo
.workbuddy/
